name: 'dbt_modular_warehouse'
version: '1.0'
profile: 'dbt_modular_warehouse'
config-version: 2

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
macro-paths: ["macros"]
analysis-paths: ["analysis"]

models:
  dbt_modular_warehouse:
    +materialized: view
    bronze:
      +schema: bronze
      +tags: [bronze]
    silver:
      +schema: silver
      +tags: [silver]
    gold:
      +schema: gold
      +tags: [gold]
