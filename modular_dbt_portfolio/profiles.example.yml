modular_dbt_portfolio:
  target: duckdb
  outputs:
    duckdb:
      type: duckdb
      path: data/duckdb.db
      schema: main
      threads: 2
    postgres:
      type: postgres
      host: localhost
      user: your_user
      password: your_password
      port: 5432
      dbname: your_db
      schema: analytics
      threads: 4
