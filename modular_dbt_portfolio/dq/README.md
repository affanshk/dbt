# Placeholder for Great Expectations integration
# To enable:
# 1. Run `great_expectations init` at repo root.
# 2. Create expectation suites for each silver table, e.g. `great_expectations suite new`.
# 3. Add/checkpoint configuration and point CI to run `great_expectations checkpoint run`.

# Example: a simple expectation suite file can live under great_expectations/expectations/<datasource>__<table>.json
